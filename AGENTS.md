# iSurvive

Do not invert: kit sales → dual host → Field Kit Operator → build-in-public.

- Margin: `python -m isurvive margin` (`price >= landed / 0.70`)
- Dual host: remotes `github` and `origin`; `python -m isurvive verify-host`
- No medical or personal data in the tree
- Do not publish X drafts; leave them in `drafts/x/`
- Stripe: Checkout Sessions only. No `payment_method_types`. No tax until a registration exists.
