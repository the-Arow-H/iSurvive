# Kits

Catalog is `catalog.json`. Drawings in `photos/` stand in until studio shots exist. A SKU needs all of: SKU, landed, photo, checkout, margin.

Process: `P` printed polymer, `X` off-the-shelf.

```bash
python -m isurvive catalog
python -m isurvive margin
```
